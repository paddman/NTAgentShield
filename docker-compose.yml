services:
  ntshield-api:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./rules:/app/rules:ro
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
